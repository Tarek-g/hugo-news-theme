# 🚀 دليل البدء السريع - صفحة المقالة المحسّنة

## ✅ التحسينات التي تم تطبيقها

### **1. استرجاع Social Share Buttons**
✅ تم العودة للأيقونات البسيطة (بدون نصوص ملونة)

```
مشاركة: [🐦] [f] [💬]
```

---

### **2. جدول محتويات محسّن**
✅ يظهر تلقائياً عند وجود أي عنوان H2

**كيفية الكتابة:**
```markdown
## عنوان جزء
محتوى...

## عنوان جزء آخر
محتوى...
```

**النتيجة:** جدول محتويات تلقائي مع روابط سريعة

---

### **3. اقرأ المزيد في Related Posts**
✅ إضافة "اقرأ المزيد ←" مع تأثير hover

```
[عنوان المقالة]
الملخص...
اقرأ المزيد ←
```

---

## 🎯 إجابات أسئلتك:

### **❓ من أين يأتي وصف الكاتب؟**

**الملف:** `i18n/ar.yaml` (السطر ~22)

```yaml
author_bio:
  other: "متخصص في الأخبار والتقارير الحصرية والمحتوى الجودة."
```

**للتعديل:** غيّر النص وأعد بناء الموقع

---

### **❓ كيف أنشئ صفحة أرشيف الكاتب؟**

**3 خطوات بسيطة:**

#### **الخطوة 1:** ملف للكاتب

أنشئ `content/authors/اسم-الكاتب.md`:

```yaml
---
title: "اسم الكاتب"
bio: "نبذة عن الكاتب..."
type: "author"
layout: "author"
---
```

#### **الخطوة 2:** Template للكاتب

أنشئ `layouts/author/single.html`:

```html
{{ define "main" }}
<div class="container mx-auto px-4 py-12">
  <h1 class="text-4xl font-black mb-4">{{ .Title }}</h1>
  <p class="text-lg text-muted-foreground mb-8">{{ .Params.bio }}</p>
  
  <h2 class="text-2xl font-bold mb-6">مقالات الكاتب</h2>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {{ range where .Site.RegularPages "Params.author" .Title }}
    <a href="{{ .RelPermalink }}" class="group block">
      <div class="bg-card p-4 rounded-sm border border-border hover:border-primary transition-colors">
        <h3 class="text-lg font-bold mb-2 group-hover:text-primary">{{ .Title }}</h3>
        <p class="text-sm text-muted-foreground">{{ .Date.Format "2006-01-02" }}</p>
      </div>
    </a>
    {{ end }}
  </div>
</div>
{{ end }}
```

#### **الخطوة 3:** الربط

في `layouts/_default/single.html` غيّر:

```html
{{ with .Params.author }}
<a href="/authors/{{ . | urlize }}/">{{ . }}</a>
{{ end }}
```

---

### **❓ لماذا لم أر جدول المحتويات في `opinion-3`؟**

**السبب:** المقالة قد لا تحتوي على عناوين H2

**الحل:** أضف عناوين في الكتابة:

```markdown
## الفكرة الأولى
...

## الفكرة الثانية
...
```

✅ سيظهر جدول المحتويات تلقائياً

---

### **❓ كيف أعدّل صندوق اشترك النشرة؟**

**الملف 1:** `hugo.toml`

```toml
[params]
  newsletter_url = "/newsletter/"
  newsletter_title = "عنوان مخصص"
```

**الملف 2:** `i18n/ar.yaml`

```yaml
newsletter_title:
  other: "هل استفدت من المقالة؟"
newsletter_description:
  other: "اشترك لتصلك المزيد من الأخبار"
subscribe:
  other: "نعم، اشترك الآن"
```

---

### **❓ كيف يمكن للمحرر تعديل الأشياء بسهولة؟**

#### **✅ سهل جداً (نصوص):**

فقط عدّل `i18n/ar.yaml`:

```yaml
# جميع النصوص في مكان واحد
newsletter_title: "..."
newsletter_description: "..."
table_of_contents: "..."
author_bio: "..."
share: "..."
by: "..."
```

#### **✅ سهل (الإعدادات):**

فقط عدّل `hugo.toml`:

```toml
[params]
  newsletter_url = "..."
  
[[menus.main]]
  name = "قسم جديد"
  url = "/path"
```

#### **⚠️ متقدم (التصميم):**

عدّل ملفات HTML في `layouts/`

---

## 📁 دليل الملفات المهمة

```
hugo-news-theme/
├── hugo.toml                    ← ⭐ إعدادات الموقع
├── i18n/
│   └── ar.yaml                 ← ⭐ جميع النصوص
├── layouts/
│   ├── _default/
│   │   ├── single.html          ← صفحة المقالة
│   │   ├── list.html            ← قائمة المقالات
│   │   └── taxonomy.html        ← قائمة الأقسام
│   ├── partials/
│   │   └── sidebar.html         ← الشريط الجانبي
│   └── index.html               ← الصفحة الرئيسية
├── content/
│   ├── posts/                   ← المقالات
│   ├── opinion/                 ← مقالات الرأي
│   └── authors/ (جديد)          ← أرشيف الكتاب
└── static/
    └── images/                  ← الصور
```

---

## 🔧 الأوامر الأساسية

```bash
# بناء الموقع
hugo -D

# عرض الموقع مباشرة
hugo server -D

# تنظيف الملفات المؤقتة
hugo mod clean
```

---

## 📝 ملخص سريع

| الحاجة | الملف | الصعوبة |
|-------|------|--------|
| تعديل النصوص | `i18n/ar.yaml` | ⭐ سهل |
| تعديل الإعدادات | `hugo.toml` | ⭐ سهل |
| تعديل التصميم | `layouts/**` | ⭐⭐⭐ متقدم |
| إضافة مقالات | `content/posts/` | ⭐ سهل |
| إضافة أرشيف كاتب | ملف جديد | ⭐⭐ متوسط |

---

## 🎨 نصائح للمحررين

### 1. عند كتابة مقالة جديدة:
```yaml
---
title: "العنوان"
author: "اسم الكاتب"
categories: ["القسم"]
tags: ["وسم1", "وسم2"]
date: 2026-01-23
---

## الجزء الأول
محتوى...

## الجزء الثاني
محتوى...
```

### 2. استخدم Markdown بشكل صحيح:
- `#` = H1 (العنوان الرئيسي - لا تستخدمه)
- `##` = H2 (عناوين الأجزاء - يظهر في TOC)
- `###` = H3 (عناوين فرعية)
- `**نص**` = نص غامق
- `*نص*` = نص مائل

### 3. جدول المحتويات يتم بناؤه تلقائياً من:
- جميع عناوين `##` في المقالة
- يجب أن يكون هناك عنوان واحد على الأقل

---

## 🚀 الخطوات التالية

هل تريد:
- [ ] إضافة صفحة أرشيف الكاتب فعلاً الآن؟
- [ ] إضافة نماذج اتصال؟
- [ ] تحسين محرك البحث؟
- [ ] إضافة تعليقات؟
- [ ] نظام إشعارات البريد؟

اختر واحدة وسأساعدك!

