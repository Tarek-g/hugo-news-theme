# 📝 دليل المحرر - كيفية التحكم بصفحة المقال والموقع

## 📌 فهرس سريع
1. [وصف الكاتب (Author Bio)](#author-bio)
2. [جدول المحتويات (Table of Contents)](#table-of-contents)
3. [صندوق الاشتراك (CTA Box)](#cta-box)
4. [صفحة أرشيف الكاتب](#author-archive)
5. [كل الملفات وأماكن التعديل](#file-locations)

---

## <a name="author-bio"></a>1️⃣ **وصف الكاتب (Author Bio)**

### من أين يأتي الوصف؟

الوصف يظهر في صندوق المعلومات الكاتب أسفل المقالة:

```
✍️ اسم الكاتب
متخصص في الأخبار والتقارير الحصرية والمحتوى الجودة.
```

### كيفية التعديل؟

**الطريقة 1: التعديل العام (نفس الوصف لجميع الكتاب)**

اذهب إلى ملف `i18n/ar.yaml`:

```yaml
author_bio:
  other: "ضع الوصف الجديد هنا"
```

**الطريقة 2: وصف مختلف لكل كاتب (متقدم)**

في المستقبل يمكن إضافة ملف `data/authors.yaml`:

```yaml
authors:
  "اسم الكاتب":
    name: "اسم الكاتب"
    bio: "وصف شخصي خاص به"
    email: "email@example.com"
```

ثم تعديل الـ template ليقرأ من هنا.

---

## <a name="table-of-contents"></a>2️⃣ **جدول المحتويات (Table of Contents)**

### **المشكلة:** لم أجد جدول المحتويات في المقال

### **الحل:**

جدول المحتويات يظهر **تلقائياً** إذا كان في المقالة **أكثر من عنوانين (H2)**.

**كيفية الكتابة في Markdown:**

```markdown
# العنوان الرئيسي (H1)

## الجزء الأول
محتوى...

## الجزء الثاني
محتوى...

### تحت فرع الجزء الثاني (H3 - لا يظهر في TOC)
محتوى...

## الجزء الثالث
محتوى...
```

**النتيجة:** سيظهر جدول محتويات بـ 3 عناوين (الجزء الأول، الثاني، الثالث)

### **متطلبات ظهور جدول المحتويات:**
- ✅ أكثر من 2 عنوان H2 (`## العنوان`)
- ✅ في ملف `.md`

### **مثال من مقالك:**
المقالة `opinion-3` قد لا تحتوي على عناوين بصيغة `##` (H2)، لذلك لم يظهر جدول المحتويات.

---

## <a name="cta-box"></a>3️⃣ **صندوق الاشتراك (CTA Box)**

الصندوق الأزرق أسفل كل مقالة:

```
هل أعجبك هذا المقال؟
اشترك في النشرة البريدية...
[اشترك الآن ➜]
```

### **كيفية التعديل:**

اذهب إلى `hugo.toml` وعدّل:

```toml
[params]
  newsletter_url = "/newsletter/"  # الرابط الذي يذهب إليه
```

أو عدّل النصوص في `i18n/ar.yaml`:

```yaml
newsletter_title:
  other: "النص الذي تريده هنا"
newsletter_description:
  other: "الوصف الجديد هنا"
subscribe:
  other: "النص على الزر"
```

**مثال:**
```yaml
newsletter_title:
  other: "ابق محدثاً مع أخبارنا"
newsletter_description:
  other: "اشترك لتصلك آخر التطورات يومياً"
subscribe:
  other: "نعم، أريد الاشتراك"
```

---

## <a name="author-archive"></a>4️⃣ **صفحة أرشيف الكاتب**

### الحالة الحالية:
لا توجد حالياً صفحة أرشيف للكاتب، لكن يمكن إضافتها!

### **خطة التطبيق:**

#### **الخطوة 1:** إنشاء layout جديد

اسم الملف: `layouts/authors/single.html`

```html
{{ define "main" }}
<div class="container mx-auto px-4 py-12">
  <h1 class="text-4xl font-black mb-4">{{ .Title }}</h1>
  <p class="text-muted-foreground mb-8">{{ .Params.bio }}</p>
  
  <h2 class="text-2xl font-bold mb-6">مقالات الكاتب</h2>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {{ range where .Site.RegularPages "Params.author" .Title }}
    <a href="{{ .RelPermalink }}" class="group block">
      <div class="bg-secondary/10 p-4 rounded-sm hover:bg-secondary/20 transition-colors border border-border hover:border-primary">
        <h3 class="text-lg font-bold mb-2 group-hover:text-primary">{{ .Title }}</h3>
        <p class="text-sm text-muted-foreground mb-3">{{ .Summary | truncate 100 }}</p>
        <span class="text-xs text-primary">📅 {{ .Date.Format "2006-01-02" }}</span>
      </div>
    </a>
    {{ end }}
  </div>
</div>
{{ end }}
```

#### **الخطوة 2:** إنشاء ملفات الكتاب

اسم الملف: `content/authors/اسم-الكاتب.md`

```yaml
---
title: "اسم الكاتب"
bio: "متخصص في المجال الفلاني..."
email: "email@example.com"
---
```

#### **الخطوة 3:** الربط من الملف الرئيسي

عدّل `layouts/_default/single.html`:

```html
{{ with .Params.author }}
<a href="/authors/{{ . | urlize }}/" class="text-lg font-bold">{{ . }}</a>
{{ end }}
```

---

## <a name="file-locations"></a>5️⃣ **جميع الملفات وأماكن التعديل**

### **الملفات الرئيسية للمحرر:**

| الملف | الوصف | التأثير |
|------|-------|---------|
| `hugo.toml` | إعدادات الموقع | **عام** |
| `i18n/ar.yaml` | الترجمات والنصوص | **كل صفحة** |
| `content/posts/*.md` | محتوى المقالات | **مقالات** |
| `content/opinion/*.md` | مقالات الرأي | **رأي** |
| `layouts/_default/single.html` | تصميم المقالة | **مقالات وأراء** |

### **ملفات محرر متقدمة:**

| الملف | التعديلات | الصعوبة |
|------|-----------|--------|
| `hugo.toml` | Colors, Menu, Homepage | ⭐ سهل |
| `i18n/ar.yaml` | All text strings | ⭐ سهل |
| `layouts/**/*.html` | Design & Layout | ⭐⭐⭐ متقدم |
| `assets/css/*.css` | Styling | ⭐⭐ متوسط |

---

## 🔧 **الأشياء التي يمكن للمحرر تعديلها بسهولة:**

### ✅ **سهل (في `hugo.toml`):**

```toml
# عنوان الموقع
title = "اسم الموقع الجديد"

# الألوان
[params.colors]
  primary = "221 83% 53%"  # لون أساسي جديد

# عناصر القائمة الرئيسية
[[menus.main]]
  name = 'اسم القسم'
  url = '/categories/اسم-القسم/'
  weight = 20  # الترتيب

# الصفحة الرئيسية
[params.homepage]
  hero_count = 2        # عدد المقالات الرئيسية
  sub_count = 8         # عدد المقالات الفرعية
  sidebar_latest_count = 10
```

### ✅ **سهل (في `i18n/ar.yaml`):**

```yaml
# تغيير أي نص على الموقع
newsletter_title:
  other: "النص الجديد"
  
by:
  other: "كتبه:"  # بدل "بقلـم"
```

### ⚠️ **متقدم (في HTML/CSS):**
- تغيير التصميم الكامل
- إضافة ميزات جديدة
- تعديل الألوان والخطوط

---

## 📝 **مثال عملي: تعديل نص الاشتراك**

**هدفك:** تغيير نص صندوق الاشتراك

**الحل:**
1. افتح `i18n/ar.yaml`
2. ابحث عن `newsletter_title`
3. غيّر النص:
```yaml
newsletter_title:
  other: "هل استفدت من المقالة؟ اشترك معنا!"
```
4. احفظ الملف
5. أعد بناء الموقع: `hugo -D`

✅ **تم!** النص سيتغير في جميع المقالات

---

## 📞 **ملخص الإجابات:**

### ❓ من أين يأتي وصف الكاتب؟
→ من `i18n/ar.yaml` في `author_bio`

### ❓ هل يمكن صفحة أرشيف للكاتب؟
→ نعم! (شرح كامل أعلاه في القسم 4)

### ❓ لماذا لا أرى جدول المحتويات؟
→ المقالة يجب أن تحتوي على أكثر من عنوان H2

### ❓ كيف أعدل صندوق الاشتراك؟
→ في `hugo.toml` و `i18n/ar.yaml`

### ❓ كيف يمكن للمحرر تعديل الأشياء؟
→ من `hugo.toml` و `i18n/ar.yaml` (سهل!)
→ من HTML ملفات التصميم (متقدم)

---

## 🚀 **التالي:**

هل تريد:
1. ✏️ إضافة صفحة أرشيف الكاتب فعلاً؟
2. 🎨 تخصيص مزيد من الألوان والنصوص؟
3. 📊 إضافة إحصائيات الكاتب؟
4. 🔍 محرك بحث محسّن؟

